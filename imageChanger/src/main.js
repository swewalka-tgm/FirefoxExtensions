console.log("The extension is up and running");

var images = document.getElementsByTagName("img");

// for (elem of images){
//     elem.src = `${browser.runtime.getURL("../img/replacement.jpg")}`
// }